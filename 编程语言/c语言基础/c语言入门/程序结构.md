---
tags:
  - c语言
---

## 顺序结构
程序从上到下逐行执行语句代码
## 选择结构
```c
if (逻辑表达式){    //非0为真
	代码块
}
```
如果逻辑表达式的值为真，执行代码块中的内容（负数也会执行代码块中的内容）
```c
if (){
}
else if(){
}
else {
}
```
如果所有if语句中都没有结果为真的逻辑表达式，执行else代码块中的内容
## 循环结构
**for循环**
```c
for (表达式1;表达式2;表达式3){
	循环体
}
```
先执行表达式1，再执行2，再执行3
